{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/DylanMerigaud/bankstatements/corpus/schema/transaction.schema.json",
  "title": "Transaction bancaire normalisee",
  "description": "LE SCHEMA EST UNE DONNEE, PAS DU CODE, et c'est delibere. Le meme corpus doit servir l'implementation Python et l'implementation TypeScript sans qu'aucune ne devienne la reference: deux implementations qui portent chacune leur verite divergent, et c'est ainsi que ce genre de projet meurt. Mesure qui motive ce fichier (2026-08-05): sur la meme notion de transaction, la librairie mt940 rend 37 champs et ofxparse en rend 10, avec seulement TROIS champs en commun (montant, date, identifiant). La contrepartie s'appelle applicant_name d'un cote et payee de l'autre.",
  "type": "object",
  "required": ["date", "amount", "currency", "raw"],
  "additionalProperties": false,
  "properties": {
    "date": { "type": "string", "format": "date", "description": "Date de valeur." },
    "booking_date": { "type": ["string", "null"], "format": "date" },
    "amount": {
      "type": "string",
      "description": "DECIMAL EN CHAINE, jamais un flottant. Un centime perdu dans un binary64 est un rapprochement faux, et le format d'origine porte deja des decimales exactes."
    },
    "currency": { "type": "string", "minLength": 3, "maxLength": 3 },
    "counterparty_name": { "type": ["string", "null"] },
    "counterparty_account": { "type": ["string", "null"] },
    "reference": { "type": ["string", "null"] },
    "purpose": { "type": ["string", "null"] },
    "bank_reference": { "type": ["string", "null"] },
    "type_code": { "type": ["string", "null"] },
    "raw": {
      "type": "object",
      "description": "TOUJOURS present. Les champs propres au format d'origine, tels quels. Une normalisation qui jette l'original oblige a re-parser le fichier des qu'une question sort du schema, et personne ne garde le fichier."
    }
  }
}
