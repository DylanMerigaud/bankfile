## What this changes

## The bank deviation covered, if there is one
<!-- A parsing fix without a fixture is a fix that will come back: the corpus is
     the asset, the code is not. Add the anonymised file under corpus/banks/. -->

## Checked
- [ ] `uv run ruff check . && uv run ruff format --check .`
- [ ] `uv run mypy`
- [ ] `uv run pytest`
